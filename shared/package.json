{
  "name": "asker-shared",
  "version": "1.0.0",
  "description": "Shared asker packages",
  "scripts": {
    "migrate": "prisma migrate dev",
    "generate": "prisma generate"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "prisma": "^5.20.0"
  }
}
